Contact-level data (owner names, direct mobiles, outreach sheet). Never committed, denied to agents in `.claude/settings.json`.
Anything leaving this folder for `output/public/` goes through masking.
