Immutable vendor exports, never edited in place, not committed.
Layout: `data/raw/<source_id>/<YYYY-MM-DD>__<label>.json` (Apify JSON export) or `.csv` (manual url lists).
The file stem is the `run_id`; every file has a row in `data/runs.csv`.
